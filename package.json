{
  "name": "react-google-font",
  "version": "1.0.2",
  "description": "A React component to load Google Fonts dynamically",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "repository": "https://github.com/dherault/react-google-font",
  "author": "David Hérault <dherault@gmail.com> (https://github.com/dherault)",
  "license": "MIT",
  "files": [
    "dist/**/*",
    "src/**/*"
  ],
  "scripts": {
    "build": "npx tsc && cp src/index.d.ts dist/index.d.ts",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "peerDependencies": {
    "react": "*"
  },
  "devDependencies": {
    "eslint-config-dherault": "^1.7.3",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "prop-types": "^15.8.1"
  }
}
